.vercel
.md
node_modules
.next
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.tape/

# raw personal GPS traces + intermediate street-coverage data — only the
# aggregated public/street-coverage.json output is meant to be public
data/street-coverage/
